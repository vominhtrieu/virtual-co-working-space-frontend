export interface ProxyBodyInterface {
  pushToken: string;
  device: string;
}

export interface ApiResponseInterface {
   pushToken: string;
}

export interface ProxyTransformInterface {
  pushToken: string;
}

